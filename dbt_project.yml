name: 'call_report_analytics'
version: '1.0.0'

profile: 'call_report_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  call_report_analytics:
    staging:
      +materialized: view
      +schema: TRANSFORMED
    marts:
      +materialized: table
      +schema: ANALYTICS
